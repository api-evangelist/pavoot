openapi: 3.2.0
info:
  title: Fast Delete Folder API
  version: 0.1.0
tags:
- name: DeleteFolder
paths:
  /deleteFolder:
    post:
      summary: Delete Folder Endpoint
      operationId: delete_folder_endpoint_deleteFolder_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FolderRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      tags:
      - DeleteFolder
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    FolderRequest:
      properties:
        projectId:
          type: integer
          title: Projectid
        folderName:
          type: string
          title: Foldername
      type: object
      required:
      - projectId
      - folderName
      title: FolderRequest
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
