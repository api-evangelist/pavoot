openapi: 3.2.0
info:
  title: Fast Abort Multipart Upload API
  version: 0.1.0
tags:
- name: AbortMultipartUpload
paths:
  /abortMultipartUpload:
    post:
      summary: Abort Multipart Upload Endpoint
      operationId: abort_multipart_upload_endpoint_abortMultipartUpload_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AbortMultipartUploadRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      tags:
      - AbortMultipartUpload
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    AbortMultipartUploadRequest:
      properties:
        projectId:
          type: integer
          title: Projectid
        key:
          type: string
          title: Key
        uploadId:
          type: string
          title: Uploadid
      type: object
      required:
      - projectId
      - key
      - uploadId
      title: AbortMultipartUploadRequest
