openapi: 3.2.0
info:
  title: Fast Delete Tag Bulk API
  version: 0.1.0
tags:
- name: DeleteTagBulk
paths:
  /deleteTagBulk:
    post:
      summary: Delete Tag Bulk Endpoint
      description: Deletes a tag from multiple images. Body must include imageIds, tag, category, and projectId.
      operationId: delete_tag_bulk_endpoint_deleteTagBulk_post
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
      tags:
      - DeleteTagBulk
