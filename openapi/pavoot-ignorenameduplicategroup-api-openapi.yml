openapi: 3.2.0
info:
  title: Fast Ignore Name Duplicate Group API
  version: 0.1.0
tags:
- name: IgnoreNameDuplicateGroup
paths:
  /ignoreNameDuplicateGroup:
    post:
      summary: Ignore Name Duplicate Group Endpoint
      description: Set name-duplicate suggestion status to ignored (same as face_match_suggestions).
      operationId: ignore_name_duplicate_group_endpoint_ignoreNameDuplicateGroup_post
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: true
              type: object
              title: Body
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      tags:
      - IgnoreNameDuplicateGroup
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
