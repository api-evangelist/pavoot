openapi: 3.2.0
info:
  title: Fast Get Org Potential Brand Duplicates API
  version: 0.1.0
tags:
- name: GetOrgPotentialBrandDuplicates
paths:
  /getOrgPotentialBrandDuplicates:
    get:
      summary: Get Org Potential Brand Duplicates Endpoint
      description: Propose likely-duplicate brand groups (name/keyword incl. acronym/substring/token).
      operationId: get_org_potential_brand_duplicates_endpoint_getOrgPotentialBrandDuplicates_get
      parameters:
      - name: projectIds[]
        in: query
        required: false
        schema:
          type: array
          items:
            type: integer
          default: []
          title: Projectids[]
      - name: countOnly
        in: query
        required: false
        schema:
          type: boolean
          default: false
          title: Countonly
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      tags:
      - GetOrgPotentialBrandDuplicates
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
