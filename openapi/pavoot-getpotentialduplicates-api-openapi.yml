openapi: 3.2.0
info:
  title: Fast Get Potential Duplicates API
  version: 0.1.0
tags:
- name: GetPotentialDuplicates
paths:
  /getPotentialDuplicates:
    get:
      summary: Get Potential Duplicates Endpoint
      description: Retrieves potential duplicate persons based on name similarity for a project.
      operationId: get_potential_duplicates_endpoint_getPotentialDuplicates_get
      parameters:
      - name: projectId
        in: query
        required: true
        schema:
          type: integer
          title: Projectid
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      tags:
      - GetPotentialDuplicates
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
