openapi: 3.2.0
info:
  title: Fast Delete Logo Transcription API
  version: 0.1.0
tags:
- name: DeleteLogoTranscription
paths:
  /deleteLogoTranscription:
    delete:
      summary: Delete Logo Transcription Endpoint
      operationId: delete_logo_transcription_endpoint_deleteLogoTranscription_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Body_delete_logo_transcription_endpoint_deleteLogoTranscription_delete'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
      tags:
      - DeleteLogoTranscription
components:
  schemas:
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    Body_delete_logo_transcription_endpoint_deleteLogoTranscription_delete:
      properties:
        logoId:
          type: string
          title: Logoid
        transcription:
          type: string
          title: Transcription
      type: object
      required:
      - logoId
      - transcription
      title: Body_delete_logo_transcription_endpoint_deleteLogoTranscription_delete
